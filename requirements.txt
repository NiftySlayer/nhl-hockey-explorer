# Versions this pipeline was built and run under (Python 3.13).
# Nothing is pinned tightly on purpose — these are the versions used, not the
# minimum supported.

# Scraping (scrape_raw.py, shifts_html.py, edge_scrape.py)
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2

# Building and validation (build_processed.py, stints.py, shotframe_validation.py)
numpy==2.3.5
pandas==2.3.3
pyarrow==22.0.0

# Figures only (src/figures.py). Nothing in the pipeline imports it, so the
# build and both validators run without this line installed.
matplotlib==3.11.1

# Web application / deployment
Flask>=3.1.3
boto3