[project]
name = "nhl-hockey-explorer"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.1.3",
    "boto3",
    "pandas>=2.3.3",
    "pyarrow==22.0.0"
]

[tool.vercel]
entrypoint = "web_app:app"